#include <stdio.h>
#include <math.h>
/* funcion*/
void main(void)
{
    float X;
    int Y;
    printf("Ingrese el valor de Y: ");
    scanf("%d", &Y);
if (Y < 0 ││ Y > 50)
        x = 0;
    else
        if (y <= 10)
        X = 4/ Y - Y;
    else
        if(y <= 25)
        X = pow (Y, 3) - 12;
    else
        X = pow( Y, 2) + pow(Y, 3) -18;
    printf("\n\nY = %d\tx = %8.2f", Y, X);
    return 0;
}
